# app_admin: BYPASSRLS. Migraciones, resolución de identidad, ops cross-tenant auditadas.
DATABASE_URL="postgresql://app_admin:CHANGE_ME@localhost:5432/loolo?schema=public"

# app_user: SIN bypass. Runtime tenant-scoped. RLS lo filtra todo. El dominio usa esta.
RUNTIME_DATABASE_URL="postgresql://app_user:CHANGE_ME@localhost:5432/loolo?schema=public"

# Better Auth
BETTER_AUTH_SECRET="CHANGE_ME"
BETTER_AUTH_URL="http://localhost:3000"

# URL pública de la app (usada por el cliente de auth y metadata). En producción: https://nelzzon.com
NEXT_PUBLIC_APP_URL="http://localhost:3000"
